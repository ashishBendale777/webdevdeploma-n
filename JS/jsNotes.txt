Variables==
js Datatypes 
    1. primitive ==> Number . String, Float, Boolean
    2. object ==> array, object, function

Operators=
    1. unary ===> have single opearands
        ++ , --
    2. binary ===> opearators must have 2 opearands
        1. arithmetic + - * / %
        2. assiggnmetns = += -= *= /= %=
        3. logical || && !
        4. relational > >= < <= == !=

        =====================================

        if ...else if....else
        marks= 34.54

        grades ===
            A+ => 90 +
            A  => 75 to 90
            B  => 60 to 75
            C  => 50 to 60
            D  => 35 to 50
            E  => FAIL


=============
1 print odds nums between 20 - 50
2 count nums divisible by 4, 7, 12 nums between 50-150
3 give me sum of 6 and 8 divisible nums between 200-400